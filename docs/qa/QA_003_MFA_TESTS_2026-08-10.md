# QA-003 MFA Tests

Date: 2026-08-10  
Scope: sensitive actions blocked without MFA/AAL2
Updated: 2026-08-27 for PDFGEN-008 and QA-003-MFA-RLS-001

## Summary

QA-003 passes all 31 aggregate runtime assertions. The PDFGEN-008 update
confirmed the four application roles, AAL1/AAL2 interpretation, active-profile
and multi-role MFA resolution, private RLS policies, user/role management,
learners, contacts, site settings, credential lifecycle functions, all
template/single-generation/batch-generation/activation helpers, PDF operations,
and server route guards. QA-003-MFA-RLS-001 closed the runtime-identified gap by
adding the shared profile-aware MFA helper to all 45 editorial mutation policies.

A fresh dev Owner password session was confirmed as `aal1` with a verified TOTP factor available for upgrade to `aal2`. That authenticated session was then denied by all 11 tested sensitive admin routes with HTTP `403` and an explicit `MFA/AAL2` message. No records or configuration values were changed.

## MFA Contract

| Role | Default MFA requirement | AAL1 sensitive access | AAL2 authorized access evidence |
| --- | --- | --- | --- |
| Owner | Required | Denied | Existing Owner admin/browser QA passed |
| Super Admin | Required | Denied | Stage 2–4 live role QA passed |
| Content Manager | Optional | Permitted for editorial work when the profile does not independently require MFA | Existing content CRUD QA passed |
| Credential Manager | Required | Denied | Stage 2–4 and credential-module live role QA passed |

For a user with multiple roles, any assigned MFA-required role makes the session MFA-required. The server checks both `profile.mfa_required` and the assigned role set; the database assignment trigger also forces `mfa_required` when Owner, Super Admin, or Credential Manager is assigned.

## Live AAL1 Routes

The following authenticated Owner requests were denied before any data access or mutation:

- `/api/v1/admin/users`;
- `/api/v1/admin/contact-submissions`;
- `/api/v1/admin/learners`;
- `/api/v1/admin/credentials`;
- `/api/v1/admin/credential-sets`;
- `/api/v1/admin/document-numbers`;
- `/api/v1/admin/site-settings`;
- `/api/v1/admin/content-pages`;
- `/api/v1/admin/programmes`;
- `/api/v1/admin/partners`;
- `/api/v1/admin/experts`.

`/api/v1/admin/me` remained available and correctly reported `aal1` with `mfa.satisfied = false`, proving that the denials came from MFA authorization rather than an invalid session.

## Files Changed

- `supabase/tests/database/qa_003_mfa_matrix.test.sql` — 31 aggregate pgTAP assertions.
- `supabase/tests/database/qa_003_content_policy_mfa_hardening.test.sql` — 10 focused assertions.
- `supabase/migrations/20260827100000_qa_003_content_policy_mfa_hardening.sql` — 45 policy corrections.
- `scripts/verify-qa-003.mjs` — static MFA, route-guard, RLS, function, and client-secret verifier.
- `scripts/verify-qa-003-content-policy-mfa.mjs` — static migration/inventory regression.
- `scripts/verify-qa-003-live.mjs` — non-mutating fresh-AAL1 dev test.
- `package.json` — QA-003 verification commands.
- planning/checklist documentation and this report.

## Database Objects

The local repository chain now contains a forward-only migration that alters 45
existing editorial mutation policies. It adds no table, column, function,
trigger, type, grant, Storage policy, or data row. Hosted Development and
Production were not changed.

## Tests / Verification

Passed:

- `npm run verify:qa-003`;
- `npm run verify:qa-003:live`;
- `npm run verify:auth-004`;
- `npm run verify:auth-006`;
- `npm run verify:auth-007`;
- `npm run verify:qa-001`;
- `npm run lint`;
- `npx tsc --noEmit`;
- `npm run build`;
- `git diff --check`.

Live results:

- fresh Owner session: `currentLevel = aal1`, `nextLevel = aal2`;
- at least one verified TOTP factor exists;
- admin context: authenticated, active Owner, MFA not satisfied;
- 11 of 11 sensitive routes returned `403` with an MFA/AAL2 explanation;
- no write request was sent.

On 2026-08-27, a clean local PostgreSQL 15 rebuild applied all 62 repository
migrations. The focused hardening suite passed 10/10, aggregate QA-003 passed
31/31, aggregate QA-001 passed 42/42, and all focused PDFGEN suites passed
203/203. The combined database regression passed 286/286 assertions.

## Security Notes

- AAL is read from the verified Supabase access-token claim, not client-submitted form data.
- Application roles are resolved from `user_profiles` and `user_roles`, not user-editable metadata.
- Sensitive routes establish the active actor and MFA context before protected data access.
- Database RLS/functions remain a second enforcement layer behind server guards.
  All 45 editorial content mutations now use the shared profile-aware MFA helper.
- No TOTP code, MFA secret, factor identifier, access token, password, or service-role key was printed or persisted by the test.
- The fresh test session was local to the non-persistent test client and was signed out after verification.

## Deviations / Open Questions

- A new live AAL2 challenge was not automated because it would require generating a current one-time TOTP code. AAL2 success is already documented by the existing Owner, Super Admin, Content Manager, Credential Manager, learner, and credential-module QA runs.
- Hosted Development contains the forward-only policy migration and passes the
  focused/aggregate QA-003 gate 41/41. Production remains untouched.

## Next Dependency

The separately reported hosted function-lint findings are resolved by migrations
63 and 64; hosted lint is clean at 64/64 parity. Production promotion and
PDFGEN mutation acceptance remain separately authorized steps.
