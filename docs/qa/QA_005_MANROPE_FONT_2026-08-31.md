# QA-005 Manrope Font — 2026-08-31

Status: implemented and locally verified; Preview acceptance pending

## Summary

The approved Manrope brand typeface is now loaded through `next/font/google`
as a variable font. Next.js downloads the WOFF2 files at build time and
self-hosts them from the Nobel ITBS deployment, so public browsers do not
contact Google Fonts.

The generated `--font-manrope` variable is mounted on the root `<html>` element
and used as the first family in the global font stack. `display: swap` and the
Next.js metric-adjusted fallback reduce invisible-text and layout-shift risk.

Only the shared Latin subset is preloaded on every route. The same generated
CSS self-hosts Cyrillic and Latin Extended unicode ranges, which browsers fetch
on the Ukrainian and Czech routes that actually need them. This avoids loading
all three language subsets on every English page.

## Baseline

The 2026-08-31 Production Lighthouse audit found no font request in 30 runs
because Manrope was declared only by name and `--font-manrope` was undefined.
The browser therefore rendered a platform fallback. Median mobile LCP was
1.99–2.14 seconds and CLS was 0 across the five-route matrix.

## Files Changed

- `app/layout.tsx`
- `app/globals.css`
- `scripts/verify-qa-005-manrope-font.mjs`
- `package.json`
- this report

## Database Objects

None.

## Tests / Verification

- `npm run verify:qa-005:manrope-font` — passed
- `npm run verify:qa-005:security-headers` — passed
- `npx tsc --noEmit` — passed
- `npm run lint` — passed with the pre-existing
  `components/admin-shell.tsx:174` warning only
- `npm run build` — passed
- local production-browser smoke on `/verify`, `/ua/verify`, and `/cz/verify`:
  - `document.fonts.check('16px Manrope')` returned `true` on all three routes;
  - English loaded one Latin WOFF2; Ukrainian loaded Latin plus Cyrillic; Czech
    loaded Latin plus Latin Extended;
  - no `fonts.googleapis.com` or `fonts.gstatic.com` request occurred;
  - no horizontal overflow occurred at a 390 × 844 mobile viewport;
- generated build CSS contains Manrope variable weights 200–800,
  `font-display: swap`, and the expected unicode ranges;
- the six generated self-hosted WOFF2 assets total 74,804 bytes, while an
  English first visit transfers only the 24,576-byte Latin source asset
  (approximately 25 KiB in Lighthouse transfer accounting).

### Focused Lighthouse Check

Lighthouse CLI 13.4.1 was run three times against the local production build's
mobile `/verify` route using the same default simulated mobile throttling as
the baseline audit.

| Variant | Performance median | LCP median | CLS median | Font transfer |
| --- | ---: | ---: | ---: | ---: |
| All three subsets preloaded | 96 | 2.82 s | 0 | 57.7 KiB |
| Final Latin-only preload | 97 | 2.64 s | 0 | 25 KiB |

The preload correction removed two unnecessary English font requests, improved
the median score, and preserved zero layout shift. The local result is not an
apples-to-apples comparison with the previously measured Vercel Production
median (97 score, 2.14-second LCP), so final performance acceptance remains a
Preview/Production deployment check rather than a claim based on unlike hosts.

## Security Notes

- No external Google Fonts request is made by the browser.
- No CSP expansion, secret, user data, verification token, or admin data is involved.

## Deviations / Open Questions

- The earlier audit suggested `next/font/local`; `next/font/google` was used
  instead because Next.js still downloads and self-hosts the font during build,
  while keeping the font declaration and metric fallback generated by the
  framework. Browser runtime behavior remains self-hosted.
- Preview performance acceptance remains open because local and Vercel timings
  are not directly comparable.
- This ticket does not split global CSS, optimize partner logos, or change cookie-consent rendering.

## Next Dependency

Review the Vercel Preview, then repeat the focused Vercel performance check
before merge or immediately after deployment if Preview protection prevents an
unauthenticated Lighthouse run.
